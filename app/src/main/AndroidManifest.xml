<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<!--Android Permissions -->

    <!-- Location Permission   -->
        <!-- Access fine location is for to get presise locations  (accurate withing few meters )      -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
        <!-- get approximate locations (uses wifi , network towers ) ueses less resources like battery  -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--   Internet access Permission - give access to internet (map data download,backend data sync and connetion)-->
    <uses-permission android:name="android.permission.INTERNET" />


<!--    inside "application" tag contains allthe settings and components -->
<!--    Explain
            supportsRtl - this is usefull when right to left languages like arabic
            Target api (31 is android 12 ) is what i target with this app [this is not minimum  api]-->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HiddenSriLanka"
        tools:targetApi="31">
<!--        Theam is added to styles -->

        <!-- Google Maps API Key  loaded from local.properties
             to protect api key expose via github -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${GOOGLE_MAPS_API_KEY}" />

        <!--All the activities/pages/screens  loded in here-->
        <activity
            android:name=".BaseActivity"
            android:exported="false" />
        <activity
            android:name=".LocationDetailActivity"
            android:exported="false" />
        <activity
            android:name=".AddLocationActivity"
            android:exported="false" />
        <activity
            android:name=".FetureCommingSoonActivity"
            android:exported="false" />
        <activity
            android:name=".AboutUsActivity"
            android:exported="false" />
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".FullMapViewActivity"
            android:exported="false" />
        <activity
            android:name=".LocationDetailScreenActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        <activity
            android:name=".SignUpActivity"
            android:exported="false" />
        <activity
            android:name=".OnboardingScreen2Activity"
            android:exported="false" />
        <activity
            android:name=".WelcomeActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
<!--        in here main activity is the entry point when app is open this will run first
            in intent filter "android.intent.category.LAUNCHER" this is where android tells this is the entry point of the app-->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
