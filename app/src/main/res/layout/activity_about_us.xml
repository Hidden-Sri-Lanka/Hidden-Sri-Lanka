<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:fitsSystemWindows="true"
    tools:context=".AboutUsActivity"
    tools:openDrawer="start">

    <!-- Navigation View (drawer) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/hedder"
        app:menu="@menu/main_menue" />

    <!-- Main content area
     used constrain layout-->

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="5dp">

        <!-- Toolbar -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="0dp"
            android:layout_height="81dp"
            android:background="@color/toolbar_background_color"
            android:elevation="4dp"
            android:theme="@style/ToolbarTheme"
            app:contentInsetEnd="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:menu="@menu/settings_toolbar_menu"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

        <!--        since contents are larger  (screen sizee is not enughf ) anabalin scrolling feture-->
        <ScrollView
            android:id="@+id/scrollView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:fillViewport="true"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/toolbar">

            <!-- Actual content of About us page -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">
                <!--            logo image-->
                <ImageView
                    android:id="@+id/splashImageView"
                    android:layout_width="172dp"
                    android:layout_height="122dp"
                    android:layout_gravity="center_horizontal"
                    android:layout_marginTop="30dp"
                    android:scaleType="centerCrop"
                    app:srcCompat="@drawable/logo_only" />

                <!--            Text in About Page-->
                <TextView
                    android:id="@+id/txtWelcome"
                    style="@style/AboutPage.TextView.Title"
                    android:text="Hidden Sri Lanka" />

                <TextView
                    android:id="@+id/txtMission"
                    style="@style/AboutPage.TextView.Mission"
                    android:text="Mission 🎯" />

                <TextView
                    android:id="@+id/txtaboutParagraph"
                    style="@style/AboutPage.TextView.Paragraph"
                    android:text="@string/aboutParagraph" />

                <TextView
                    android:id="@+id/devBy"
                    style="@style/AboutPage.TextView.Paragraph"
                    android:fontFamily="sans-serif-black"
                    android:text="@string/devBy" />

                <!-- Links to other pages
                like privacy policy,tos,contact ...-->

                <com.google.android.material.button.MaterialButton
                    style="@style/AboutPage.Button.Link"
                    android:onClick="privacyPolicy"
                    android:text="Privacy Policy" />

                <com.google.android.material.button.MaterialButton
                    style="@style/AboutPage.Button.Link"
                    android:onClick="Terms"
                    android:text="Terms of Service" />

                <com.google.android.material.button.MaterialButton
                    style="@style/AboutPage.Button.Link"
                    android:onClick="Contact"
                    android:text="Contact Us: asitha.contact.me@gmail.com"
                    android:layout_marginBottom="50dp"/>

            </LinearLayout>
        </ScrollView>
    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.drawerlayout.widget.DrawerLayout>